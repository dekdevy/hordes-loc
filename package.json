{
  "name": "hordes-loc",
  "description": "Hordes.io string localization",
  "main": "index.js",
  "scripts": {
    "generate": "rollup --config && node ./dist/build.js",
    "find-missing": "rollup --config && node ./dist/missing.js",
    "test": "eslint ./build.js ./loc ./random && npm run generate",
    "test-missing":"eslint ./missing.js ./loc ./random && npm run find-missing",
    "lint": "eslint ./build.js ./missing.js ./loc ./random --fix"
  },
  "author": "Hordes.io",
  "license": "UNLICENSED",
  "dependencies": {
    "rollup": "^1.9.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-sort-imports-es6-autofix": "^0.4.0"
  }
}
