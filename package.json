{
  "name": "hordes-loc",
  "description": "Hordes.io string localization",
  "main": "index.js",
  "scripts": {
    "generate": "rollup --config && node ./dist/build.js",
    "verbose": "rollup --config && node ./dist/build.js -v",
    "test": "eslint ./build.js ./loc && npm run generate",
    "testverbose": "eslint ./build.js ./loc && npm run generate",
    "lint": "eslint ./build.js ./loc --fix"
  },
  "author": "Hordes.io",
  "license": "UNLICENSED",
  "dependencies": {
    "rollup": "^1.9.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-sort-imports-es6-autofix": "^0.4.0"
  }
}
